# Optional. Without a key the app runs and every LLM feature reports status "disabled".
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional limits for a public deployment (docs/08-deployment.md, "Rate limits").
# API_REQUESTS_PER_CLIENT_MINUTE=300
# LLM_CALLS_PER_CLIENT_MINUTE=20
# LLM_CALLS_PER_CLIENT_DAY=200
# LLM_CALLS_PER_DAY=2000
