services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-4040}:4040"
    env_file:
      - path: .env
        required: false
    # Keeps the day's OpenAI spend across rebuilds, so the daily budget holds.
    volumes:
      - state:/app/data/state

volumes:
  state:
